# workflow

## commands

run files in venv

```
.venv/bin/python main.py
```


## git worktrees

when asked to work on git worktrees
- claude agents creates worktrees in `.claude/worktrees/`
- gpt agents creates worktrees in `.gpt/worktrees/`
